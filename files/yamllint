---
extends: default
ignore: |
  files/*
  meta/*

rules:
  line-length:
    max: 160 # 80 isn't enough
  comments:
    min-spaces-from-content: 1 # Default of 2 is annoying and "ansible-lint --write" puts it back to 1
